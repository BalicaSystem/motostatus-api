version: '3'

services:
  api-solid-pg:
    image: postgres:17
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=motostatus